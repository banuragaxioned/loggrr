export enum EProjectTable {
    name = "name", //project name
    client = "client", // budget
    status = "status", // logged
    owner = "owner", // project lead
    archive = "archive" // archive
  }